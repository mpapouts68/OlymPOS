<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:treeviewengine="clr-namespace:Syncfusion.TreeView.Engine;assembly=Syncfusion.Maui.TreeView"
             xmlns:syncfusion="clr-namespace:Syncfusion.Maui.TreeView;assembly=Syncfusion.Maui.TreeView"
             xmlns:local="clr-namespace:OlymPOS"
             x:Class="OlymPOS.FlyoutCategoriesView">

    <ContentView.BindingContext>
        <local:FlyoutCategoriesView />
    </ContentView.BindingContext>

    <ContentView.GestureRecognizers>
        <TapGestureRecognizer NumberOfTapsRequired="1" Tapped="OnFlyoutBackgroundTapped" />
        <SwipeGestureRecognizer Direction="Right" Swiped="OnFlyoutSwiped" />
    </ContentView.GestureRecognizers>

    <syncfusion:SfTreeView x:Name="treeView"
                       Margin="{OnPlatform WinUI='0,0,1,0', MacCatalyst='8,8,1,0'}"  
                       ItemsSource="{Binding ProductCategories}"
                       AutoExpandMode="None"
                       FullRowSelect="True"
                       ItemHeight="{OnPlatform Android=40, MacCatalyst=40, WinUI=40, iOS=48}"
                       ExpandActionTarget="Node">
        <!-- Embedding HierarchyPropertyDescriptors here -->
        <syncfusion:SfTreeView.HierarchyPropertyDescriptors>
            <treeviewengine:HierarchyPropertyDescriptor TargetType="{x:Type local:ProductGroup}" ChildPropertyName="Subcategories"/>
        </syncfusion:SfTreeView.HierarchyPropertyDescriptors>
        <!-- ItemTemplate for displaying each item -->
        <syncfusion:SfTreeView.ItemTemplate >
            <DataTemplate>
                <Grid x:Name="grid" RowSpacing="0" BackgroundColor="Transparent">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="40" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Grid Grid.Column="0">
                        <!-- Placeholder for icons or images if needed -->
                    </Grid>
                    <Grid Grid.Column="1" Padding="1,0,0,0" VerticalOptions="Center">
                        <Label Text="{Binding Description}"
                           FontSize="14"
                           VerticalTextAlignment="Center" />
                    </Grid>
                </Grid>
            </DataTemplate>
        </syncfusion:SfTreeView.ItemTemplate>
    </syncfusion:SfTreeView>
</ContentView>
